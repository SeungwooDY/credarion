/**
 * Shared UI surface tokens.
 *
 * `CARD` is the single source of truth for a primary card surface across the
 * app — a neutral border, soft radius, no shadow. Append layout utilities
 * (padding, overflow, margins) per usage, e.g. `className={`${CARD} p-5`}`.
 */
export const CARD = "rounded-lg border border-border bg-card";
